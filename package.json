{
  "name": "nyc-fintech-women",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "verify": "node tools/snapshot.mjs",
    "verify:render": "node tools/render-parity.mjs",
    "verify:self-test": "node tools/snapshot.mjs --self-test",
    "test": "node --test tools/*.test.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "parse5": "^8.0.1"
  }
}
